version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam
    - nolintlint
  settings:
    staticcheck:
      checks: ["all"]

formatters:
  enable:
    - gofmt
    - goimports
